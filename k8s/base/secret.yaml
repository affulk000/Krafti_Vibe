apiVersion: v1
kind: Secret
metadata:
  name: kraftivibe-secret
  namespace: kraftivibe
  labels:
    app: kraftivibe
type: Opaque
stringData:
  # Database credentials
  DB_USER: "postgres"
  DB_PASSWORD: "postgres"

  # Redis credentials
  REDIS_PASSWORD: "CHANGE_ME_IN_PRODUCTION"

  # MinIO credentials
  MINIO_ACCESS_KEY: "minioadmin"
  MINIO_SECRET_KEY: "CHANGE_ME_IN_PRODUCTION"

  # Zitadel Auth (Update with your values)
  ZITADEL_DOMAIN: "zitadel.localhost"
  ZITADEL_KEY_PATH: "./344424221306257411.json"
